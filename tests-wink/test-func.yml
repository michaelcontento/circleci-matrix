env:
    - A=A
    - B=B

command:
    # Using bash try/catch see: http://stackoverflow.com/questions/22009364/is-there-a-try-catch-command-in-bash
    # test that a non-exported func in circleci-matrix.sh is not exposed
    - { output=$(func "astrg" 2>&1) && { echo "ERROR: expected func to error, but it succeeded output=$output"; exit 1; } } || { echo "OK"; }

